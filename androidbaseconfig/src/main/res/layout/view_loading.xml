<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:background="@android:color/transparent"
    android:layout_height="wrap_content"
    android:orientation="horizontal" >

    <LinearLayout
        android:layout_width="wrap_content"
        android:background="@drawable/loading_dialog_shape"
        android:layout_height="wrap_content"
        android:paddingLeft="12dp"
        android:paddingRight="12dp"
        android:paddingTop="2dp"
        android:paddingBottom="2dp"
        android:layout_centerHorizontal="true"
        android:orientation="horizontal" >

        <ProgressBar
            android:id="@+id/define_porgress_progressBar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            style="@style/loading_progressbar_style"
            android:layout_gravity="center_vertical" />

        <TextView
            android:id="@+id/define_porgress_tvMessage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:layout_marginLeft="12dp"
            android:text="@string/loading"
            android:textColor="@color/normal_txt_color"
            android:textSize="@dimen/normal_txt_size" />
    </LinearLayout>

</RelativeLayout>